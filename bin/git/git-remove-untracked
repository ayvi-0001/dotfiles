#!/usr/bin/env bash

set -euo pipefail

git fetch --prune \
  && git branch -r \
  | awk "{print \$1}" \
  | grep -Evf /dev/fd/0 <(git branch -vv | grep origin) \
  | awk "{print \$1}" \
  | xargs git branch -d

exit 0
